# pycookbook 依赖清单(带版本号)
# 权威依赖源是 pyproject.toml;本文件是给习惯 `pip install -r` 的人的便捷镜像。
#
# 装全部:      pip install -r requirements.txt
# 只跑纯语法:  仅需下面「核心」一段;领域依赖(backend/data/ai)用不到可注释掉。

# ───── 核心(几乎每个配方都可能用)─────
rich>=13.0

# ───── 开发工具链(对应 10_ops/:代码质量 + 测试)─────
ruff>=0.6
mypy>=1.11
pytest>=8.0
pytest-cov>=5.0
hypothesis>=6.0

# ───── 04_libraries 常用三方库 ─────
httpx>=0.27
pydantic>=2.0
loguru>=0.7
tenacity>=9.0

# ───── 07_backend ─────
fastapi>=0.115
uvicorn>=0.30
sqlalchemy>=2.0
redis>=5.0

# ───── 08_data ─────
numpy>=2.0
pandas>=2.2
matplotlib>=3.9
pyarrow>=15.0

# ───── 09_ai(依赖最重;torch 请按平台从官网单独装)─────
scikit-learn>=1.5
transformers>=4.44
